{
  "datacenter": "{{ pillar['consul']['datacenter'] }}",
  "data_dir": "{{ pillar['consul']['data_dir] }}",
  "server": true,
  "ui_dir": "/usr/local/bin/consul_ui/dist",
  "bootstrap_expect": 2,
  "start_join": {{ pillar['consul']['start_join'] }}
}
